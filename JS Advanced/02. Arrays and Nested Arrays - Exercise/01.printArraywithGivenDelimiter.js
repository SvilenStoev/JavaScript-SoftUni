function arrayPrint(array, delimeter) {
    console.log(array.join(delimeter));
}

arrayPrint(['One', 
            'Two', 
            'Three', 
            'Four', 
            'Five'], 
            '-');